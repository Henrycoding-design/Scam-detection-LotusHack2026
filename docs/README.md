# ScamShield Extension

Real-time scam detection for every page you visit.

## Development

**Prerequisites:** Node.js

1. Install dependencies:
   ```
   npm install
   ```
2. Configure API keys in `scam-shield/background.js`:
   - Replace `BUILTIN_GSB_KEY_PLACEHOLDER` with your Google Safe Browsing API key
   - Replace `BUILTIN_VT_KEY_PLACEHOLDER` with your VirusTotal API key
   - Replace `BUILTIN_OPENROUTER_KEY_PLACEHOLDER` with your OpenRouter API key
   - Replace `BUILTIN_OPENAI_KEY_PLACEHOLDER` with your OpenAI API key (free Moderation API)
3. Build the dashboard:
   ```
   npm run build
   ```
4. Load the extension in Chrome:
   - Open `chrome://extensions/`
   - Enable "Developer mode"
   - Click "Load unpacked" and select the `scam-shield` folder
5. Open the side panel (click extension icon → side panel opens) to view the dashboard.

## Project Structure

```
├── dashboard.html              # Vite entry point (dev)
├── src/                        # Dashboard source (React + TypeScript + Tailwind)
│   ├── main.tsx                # React root
│   ├── App.tsx                 # Dashboard layout, filtering, detail modal
│   ├── chrome.ts               # Chrome runtime messaging wrapper
│   ├── types.ts                # Shared TypeScript types (ElementData, PageContext)
│   ├── utils.ts                # Status colors, risk colors, type icons
│   └── components/
│       ├── ElementCard.tsx      # Individual element card
│       └── StatsRow.tsx         # Stats summary row
├── scam-shield/                # Chrome extension directory
│   ├── manifest.json           # Extension manifest (MV3)
│   ├── background.js           # Service worker: API scanning, IndexedDB, messaging
│   ├── content/scanner.js      # Content script: DOM scanning, text analysis, bubble UI
│   ├── popup.html              # Extension popup
│   └── assets/                 # Built dashboard output (generated by `npm run build`)
├── docs/
│   ├── README.md               # This file
│   └── requirements.md         # Project requirements and design constraints
├── package.json
├── tsconfig.json
└── vite.config.ts              # Vite config, outputs build to scam-shield/assets/
```

## Features

- Scans all links, buttons, downloads, forms, iframes, and media in real-time
- Detects crypto addresses (ETH, BTC, SOL, TRX, LTC, XRP) on any page
- Monitors clipboard for address hijacking attempts
- Scans page text for scam phrases, phishing language, homoglyph attacks
- Flags forms requesting seed phrases, private keys, or recovery phrases
- MutationObserver-based — fully event-driven, zero polling
- SHA-256 content hashing — each unique text scanned exactly once
- Concurrent scanning with batching and IndexedDB storage
- Google Safe Browsing + VirusTotal + OpenAI Moderation + Veritas AI
- AI-powered explanations via OpenRouter (stepfun/step-3.5-flash:free)
- Blocking UI with popup warnings and "Visit Anyway" override
- Dashboard with live statistics and element list
- Push-based updates via chrome.runtime.Port
- Download interception with file:// navigation blocking
